{% extends 'base.html.twig' %}

{% block body %}
<h1>Créer un chocolat</h1>

{{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.price) }}

    <button class="btn">Créer</button>

{{ form_end(form) }}
{% endblock %}
